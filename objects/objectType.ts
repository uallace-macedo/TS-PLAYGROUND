/**
 * Objects
 * 
 * Tipo de dados estruturados que representam uma coleção de propriedades
 * cada uma com uma chave associada a um valor.
 * 
 * As propriedades podem ter tipos específicos e o próprio objeto
 * pode ser anotado com um tipo, geralmente definido usando "type"/"interface" aliases.
 */

const person1: { firstName: string; lastName: string; age: number } = {
  firstName: "John",
  lastName: "Santos",
  age: 20
};
