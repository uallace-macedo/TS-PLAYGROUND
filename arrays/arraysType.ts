/**
 * Arrays Type
 * 
 * Um tipo de objeto que pode armazenar múltiplos valores
 * de um mesmo tipo de dados
 */

// notação: []
const numbers: number[] = [1, 2, 3, 4];

// generic: Array<types>
const names: Array<string> = ["Alice", "Bob"];

const items: string[] = [];
items.push("Keyboard");
console.log(items);